var images = ["image1.jpg", "image2.jpg", "image3.jpg"];

images.forEach(function(img) {
  // Perform actions with each image, such as uploading to HTML and CSS
  // Example: document.getElementById("myDiv").style.backgroundImage = "url('" + img + "')";
});
